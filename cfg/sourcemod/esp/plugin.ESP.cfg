// This file was auto-generated by SourceMod (v1.11.0.6949)
// ConVars for plugin "ExtraSpawnPointsRevived.smx"


// 1 = Add the spawn points as soon as a ConVar is changed.
// -
// Default: "0"
sm_ESP_auto "0"

// 1 = When T or CT spawns are at 0, the opposite team will get double the spawn points.
// -
// Default: "1"
sm_ESP_course "1"

// 1 = Enable debugging.
// -
// Default: "0"
sm_ESP_debug "0"

// Duration (in seconds) of glow markers for sm_esp_drawspawns.
// -
// Default: "5.0"
sm_ESP_draw_duration "5.0"

// 0 = Disable plugin logic (remove extra spawns), 1 = Enable plugin logic.
// -
// Default: "1"
sm_ESP_enabled "1"

// The delay of the timer on map start to add in spawn points.
// -
// Default: "1.0"
sm_ESP_mapstart_delay "1.0"

// Max distance from team spawn centroid to use when duplicating spawns (0.0 = disabled).
// -
// Default: "0.0"
sm_ESP_maxdist "0.0"

// Minimum number of seconds between spawn rebuilds (0.0 = no limit).
// -
// Default: "1.0"
sm_ESP_min_interval "1.0"

// 0 = Only add extra spawns (minimum enforcement). 1 = Enforce exact spawn counts (may remove some map spawns).
// -
// Default: "0"
sm_ESP_replace "0"

// 1 = Validate new spawn positions with a hull trace to avoid stuck/invalid spots.
// -
// Default: "1"
sm_ESP_safetytrace "1"

// Amount of spawn points to enforce on the CT team.
// -
// Default: "32"
sm_ESP_spawns_ct "32"

// Amount of spawn points to enforce on the T team.
// -
// Default: "32"
sm_ESP_spawns_t "32"

// 0 = Disabled, 1 = All Teams, 2 = Terrorist only, 3 = Counter-Terrorist only.
// -
// Default: "1"
sm_ESP_teams "1"

// Extra Spawn Points version.
// -
// Default: "1.3"
sm_ESP_version "1.3"

// Increase Z axis by this amount when spawning a spawn point. This may resolve some issues.
// -
// Default: "16.0"
sm_ESP_zaxis "16.0"


